"""Nodes are discovered by comfy-env's register_nodes via the
sub-packages render/ (main process) and sam3d/ (isolated env).
This file is intentionally empty so register_nodes falls through to
its subdirectory-scan branch."""
